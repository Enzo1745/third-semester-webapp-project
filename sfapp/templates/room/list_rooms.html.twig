{% extends 'base.html.twig' %}

{% block title %}Room List{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/list_rooms.css') }}">
{% endblock %}

{% block body %}
    {# Main header section with gradient background #}
    <header class="bg-primary bg-gradient text-white py-4 mb-4 shadow">
        <div class="container d-flex justify-content-between align-items-center">
            <h1 class="h2 mb-0 fw-bold">
                <i class="fas fa-building me-2"></i> Room List
            </h1>
            <div>
                <a href="/charge/salles/ajouter" class="btn btn-outline-light me-2">
                    <i class="fas fa-plus-circle me-1"></i> Add a Room
                </a>
                <a href="/charge/gestion_sa" class="btn btn-outline-light">
                    <i class="fas fa-link me-1"></i> Associate/Disassociate SA
                </a>
                <a href="/charge/salles/normes" class="btn btn-outline-light">
                    <i class="bi bi-sliders2-vertical"></i> Norms
                </a>
            </div>
        </div>
    </header>

    {# Flash messages #}
    {{ block('flash_messages') }}

    {# Search/Filter form section #}
    <div class="container mb-4">
        <div class="card shadow-sm">
            <div class="card-body">
                <div class="d-flex align-items-center mb-3">
                    <i class="fas fa-search text-primary me-2"></i>
                    <h5 class="card-title mb-0">Filter</h5>
                </div>
                {{ form(form) }}
            </div>
        </div>
    </div>

    {# Main content - Rooms table #}
    <div class="container">
        <div class="card shadow-sm">
            {# Table header #}
            <div class="card-header bg-primary text-white py-3">
                <div class="row">
                    <div class="col-4 text-center fw-bold">
                        <i class="fas fa-door-open me-2"></i>Rooms
                    </div>
                    <div class="col-4 text-center fw-bold">
                        <i class="fas fa-info-circle me-2"></i>Details
                    </div>
                    <div class="col-4 text-center fw-bold">
                        <i class="fas fa-trash-alt me-2"></i>Delete
                    </div>
                </div>
            </div>

            {# Table body #}
            <div class="card-body p-0">
                {% if rooms is empty %}
                    <div class="text-center py-4 text-muted">
                        <i class="fas fa-inbox fa-2x mb-3"></i>
                        <p>No rooms available.</p>
                    </div>
                {% else %}
                    {% for room in rooms %}
                        <div class="row p-3 border-bottom align-items-center hover-effect">
                            <div class="col-4 text-center">
                                <i class="fas fa-door-closed text-primary me-2"></i>
                                {{ room.roomName }}
                            </div>

                            <div class="col-4 text-center">
                                <a href="{{ path('app_room_info', { 'roomName': room.roomName }) }}"
                                   class="btn btn-outline-primary btn-sm">
                                    <i class="fas fa-eye me-1"></i> View details
                                </a>
                            </div>
                            <div class="col-4 text-center">
                                <!-- Button to trigger the delete modal, with a unique ID -->
                                <button type="button" class="btn btn-outline-danger btn-sm" data-bs-toggle="modal" data-bs-target="#confirmDeleteModal{{ room.id }}">
                                    <i class="fas fa-trash-alt me-1"></i> Delete
                                </button>
                            </div>
                        </div>

                        <!-- Delete confirmation modal, with a unique ID -->
                        <div class="modal fade" id="confirmDeleteModal{{ room.id }}" tabindex="-1" aria-labelledby="confirmDeleteModalLabel{{ room.id }}" aria-hidden="true">
                            <div class="modal-dialog">
                                <!-- Move the form here -->
                                <form action="{{ path('app_room_delete', { 'id': room.id }) }}" method="post">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h5 class="modal-title" id="confirmDeleteModalLabel{{ room.id }}">Delete Confirmation</h5>
                                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                        </div>
                                        <div class="modal-body">
                                            Are you sure you want to delete the room "{{ room.roomName }}"?
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                            <button type="submit" class="btn btn-danger">Delete</button>
                                        </div>
                                    </div>
                                </form>
                                <!-- End of form -->
                            </div>
                        </div>

                    {% endfor %}
                {% endif %}
            </div>
        </div>
    </div>

{% endblock %}
